{
  "name": "@flash-sale/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --env-file=../.env.example --env-file-if-exists=../.env --watch --experimental-strip-types src/server.ts",
    "start": "node --env-file=../.env.example --env-file-if-exists=../.env --experimental-strip-types src/server.ts"
  },
  "dependencies": {
    "@fastify/static": "^10.1.4",
    "fastify": "^5.8.0",
    "kafkajs": "^2.2.4",
    "pg": "^8.16.0",
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@testcontainers/kafka": "^12.0.0",
    "@testcontainers/postgresql": "^12.0.0",
    "@testcontainers/redis": "^12.0.0",
    "@types/pg": "^8.15.0",
    "testcontainers": "^12.0.0"
  }
}
